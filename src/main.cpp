#include <iostream>
#include <string>
#include "VoiceEngine.hpp"
#include "Buffer.hpp"
#include "ft.hpp"

int	main()
{
	/*VoiceEngine p1;
	buffer buff(10);

	buff.push("salut");
	buff.push("hola");
	buff.push("halo");
	while (!buff.isEmpty())
	{
		std::string s = buff.pop();
		std::cout << s << std::endl;
		p1.test(s.c_str());
	}*/
	run_chat();
	return (0);
}
